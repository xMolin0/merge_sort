# Merge Sort (Parallel + OpenMP)

## Requirements
- C++ compiler with OpenMP support (tested with g++-15)
- Make

## Build
```bash
makemake run        
# OR manually:
./merge_sort <array_size> <num_threads>
make clean
